{
  "name": "speech-app-functions",
  "version": "1.0.0",
  "description": "Appwrite functions for Speech App",
  "private": true,
  "workspaces": [
    "ingest-speeches"
  ],
  "scripts": {
    "install:all": "npm install && npm install --workspaces"
  },
  "devDependencies": {
    "dotenv": "^16.4.7"
  }
}
